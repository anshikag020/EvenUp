## Starting the server
**Note**: run these commands in server directory
```go mod tidy```  # to clean & fetch deps
```go run evenup/main.go```

------------------ following are not required --------------------------------

## Initialise the project
```go mod init evenup```

## HOW TO INSTALL GIN
```go get -u github.com/gin-gonic/gin```

## To install postgresql driver for Go, run the following command:
```go get -u github.com/lib/pq```

```go get golang.org/x/crypto/bcrypt```

```go get github.com/google/uuid```

```go get github.com/emirpasic/gods```   for using TreeMap

# Required for loading .env file 
```go get github.com/joho/godotenv```

# ngrok
Download the latest Linux amd64 ZIP

wget https://bin.equinox.io/c/4VmDzA7iaHb/ngrok-stable-linux-amd64.zip
Unzip and install

unzip ngrok-stable-linux-amd64.zip
chmod +x ngrok
sudo mv ngrok /usr/local/bin/

Verify it’s on your PATH
which ngrok    # should print /usr/local/bin/ngrok
ngrok version  # confirms install
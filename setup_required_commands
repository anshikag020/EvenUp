## Main flow for starting the project
```go mod tidy```  # to clean & fetch deps
```go run main.go```

------------------ following are not required --------------------------------

## Initialise the project
```go mod init evenup```

## HOW TO INSTALL GIN
```go get -u github.com/gin-gonic/gin```

## To install postgresql driver for Go, run the following command:
```go get -u github.com/lib/pq```

